# SCode Frontend

## Development

1. Start the backend server first
2. Run the development server to serve

    ```
    yarn run dev
    ```

3. Open the url in the browser

    ```
    http://localhost:3000
    ```
