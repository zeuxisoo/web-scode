spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:scode;MODE=MySQL;
    username: sa
    password: ""
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
